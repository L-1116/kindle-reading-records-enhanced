# 版本记录

此记录根据整理前的版本目录和发布说明汇总。详细实现可检出对应 Git 标签查看。

## v9.7.5-测试版 — 2026-09-19

- 本地封面统一为“持久缓存 → Kindle `cc.db` 缩略图 → EBOK 确定路径 → EPUB/MOBI 本体”的按需 fallback；同一次数据库快照同时取得书籍实际路径，不扫描缩略图目录。
- EPUB 通过系统已有 `unzip` 读取 container/OPF，并按 EPUB3 `cover-image`、EPUB2 metadata cover、guide 与常见 cover manifest 项的顺序提取；MOBI 通过小型 Lua 解析 PalmDB/MOBI/EXTH 的正式 cover/thumbnail 记录。成功封面与失败签名均持久缓存，单个异常文件不会阻塞页面。
- PDF 不捆绑 Poppler、MuPDF 等渲染器；有 Kindle thumbnail 时继续显示，否则稳定回退默认封面。
- 阅读书籍增加“全部”，直接复用启动期全历史聚合并继续执行单书至少 5 分钟规则、既有排序和分页。
- 累计时长增加“全部”，总时长/天数读取启动摘要，图表从会话级每日缓存按年聚合；历史跨度超过 12 段时合并较早年份，不重扫原始日志。
- 启动器资源改为 600×960 的 5:8 竖版书封面，等比保留原翻开书与“阅读记录”主体并裁减外围留白；未新增字体或运行时图像依赖。
- 发布目录与版本标记更新为 `9.7.5-test`；保留 `reading-time.tsv`、原设置、9.7.4 发布目录和既有封面映射，支持原地覆盖升级。

## v9.7.4 — 2026-09-14

- 正式整合并完善 9.7.2 / 9.7.3 测试阶段的二级统计与单书详情，加入后续封面、布局和启动器改进；当前安装包已完成 Kindle 实机测试。
- 恢复 `v9.7.1-install-fix` 的完整安装入口：全新设备先完成基础安装再进入 9.7.4，已有用户直接走原地升级，不需要逐版安装。
- 为 `/mnt/us/documents/阅读记录.sh` 增加 SH_Integration Scriptlet 元数据，并随包安装轻量 PNG 启动器图标；图标在启动器替换和 Kindle 扫描前原子发布，失败时随启动器一并回滚。
- 新增统一的本地封面解析入口与 `/mnt/us/reading-time/book-cover-cache.tsv` 最后成功映射缓存；缓存失效时复用一次性只读 `cc.db` 快照，仍无结果时显示固定 2:3 默认封面。
- 阅读书籍页改为每页 3 本的“封面 + 两行书名 + 时长 + 进度 + 进度条”布局，保留原筛选、排序和上一页/下一页。
- 单书详情改为左侧封面、右侧 2×3 指标；删除最近 30 天柱状图，新增完整月份的单书阅读日历、月份切换和点击日期小提示。
- 某日阅读详情在会话派生数据中保留 `book_id/book_no`，改为每页 3 本的统一封面条目；持久阅读记录格式不变。
- 最近 8 周顶部六项统计改为等宽、在卡片内完整居中的 3×2 网格，并覆盖长时长文本；移除当前周柱状图的额外强调外框。
- 不新增联网、电子书解析、目录扫描、数据库写入或常驻依赖；计时 daemon、安装入口与历史 TSV 保持原逻辑。

## v9.7.3-测试版 — 2026-09-11

- 阅读书籍页的五个可见行现在均可点击，并由 Shell 按当前筛选和分页解析为稳定的会话内 `book_no`。
- 新增按需单书详情页，集中展示累计阅读、阅读天数、首次/最近阅读、活跃日均、本月阅读和已有 Kindle 阅读进度。
- 新增包含今天在内的连续 30 个自然日趋势，明确保留无阅读日期的零值柱位。
- `DAY_BOOKS` 会话缓存追加 `id` 与 `book_no`，同名不同 ID 不混合、同 ID 的历史标题仍聚合为同一本书；原始 `reading-time.tsv` 格式不变。
- 返回书籍页会恢复进入详情前的筛选和页数；单书聚合只发生在点击后，不增加一级页面切换或启动期的历史扫描。

## v9.7.2-测试版 — 2026-09-11

- 新增可复用的轻量时间区间聚合逻辑，只读取启动时生成的会话缓存。
- 新增月份详情页，支持从月历标题和年度月份柱进入，展示核心指标、Top 3 与每日趋势。
- 新增最近 8 周趋势，并以加粗边框标识当前周。
- 二级页面按需计算并恢复来源页面状态，不改变原始数据格式、后台计时或一级页面启动计算。

## v9.7.1-day-detail — 2026-09-11

- 修复跨月份选中日期边框，并增加可从月历和本周柱状图进入的当天详情页。
- 当天书籍按时长排序，支持分页、占比和来源页面状态恢复。

## v9.6.10-ui-layout-fix — 2026-09-10

- 修正日历热力、周视图和累计页的布局与交互细节。

## v9.6.9-calendar-heatmap — 2026-09-06

- 为月历加入分档灰度阅读热力显示。

## v9.6.8-summary-sync-fix — 2026-09-06

- 修复页面摘要与筛选统计之间的同步问题。

## v9.6.7-week-view — 2026-09-06

- 累计页增加本周视图和周切换能力。

## v9.6.6-stats-filters — 2026-09-06

- 阅读书籍与统计页面增加 7 天、月度和年度筛选。

## v9.6.5-ui-polish — 2026-09-05

- 调整字体层级、标题换行、图表留白和页面视觉一致性。

## v9.6.4-ui-calendar — 2026-09-05

- 引入周一起始的完整月历布局、日期选择和每日摘要区域。

## v9.6.3-optimized-pw6-5.19.6-cmp-fix — 2026-09-03

- 将安装器守护进程校验改为 `cmp -s`，兼容 PW6 / 5.19.6。

## v9.6.3-optimized — 2026-09-03

- 使用 Lua/PGM 合成和会话缓存减少 FBInk 调用与重复数据扫描。

## v9.6.3 — 2026-09-03

- 授权导入的上游基线；包含前台亮屏计时、书名归一化和自适应界面基础。
