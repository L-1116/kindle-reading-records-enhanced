# Kindle 阅读记录 9.6.3-optimized 测试报告

## 后台一致性

| 对象 | SHA256 | 结果 |
|---|---|---|
| 原始 9.6.3 `native-reading-time-daemon.sh` | `53C2B0472B758E0C1620835E6F8F2D26BA11D04122D982795309FEBCFE81D0EC` | 基准 |
| 优化版载荷 `native-reading-time-daemon.sh` | `53C2B0472B758E0C1620835E6F8F2D26BA11D04122D982795309FEBCFE81D0EC` | 通过，逐字节一致 |

优化版未增加 Upstart job、daemon、定时任务或后台轮询。新 Lua
渲染器只由打开的 Dashboard 同步执行；会话缓存路径为
`/tmp/native-reading-dashboard.<pid>/`，统一退出处理会删除该精确目录。

以上 SHA256 仅由电脑端离线工具计算。PW6 5.19.6 已确认本机
`sha256sum` 会崩溃，因此安装器不再执行任何 SHA256 程序，改用系统
`cmp` 做逐字节一致性校验。

## PW6 5.19.6 安装兼容修复

安装器中的三处 daemon 校验统一为：

1. 暂存前：优化包载荷与当前已安装的原始 9.6.3 daemon 逐字节一致。
2. 暂存后：暂存 daemon 与已经验证的载荷逐字节一致。
3. 激活后：最终安装 daemon 与已经验证的载荷逐字节一致。

当前已安装 daemon 不存在或任意一次 `cmp -s` 不一致时，安装失败。
第一项检查位于暂存、停止服务及文件激活之前，因此本包明确作为原始
9.6.3 的原地升级包使用。安装器中不存在 `sha256sum`、
`busybox sha256sum` 或 `openssl dgst -sha256` 调用。

## 已完成的离线检查

- [x] 原 daemon SHA256 与基准一致。
- [x] 原版 daemon 文件未修改。
- [x] 安装器三处 daemon 一致性检查均使用 `cmp -s`。
- [x] 安装器不含 `sha256sum`、BusyBox SHA 或 OpenSSL SHA 调用。
- [x] 当前已安装 daemon 缺失时在暂存和停止服务之前拒绝安装。
- [x] 离线行为测试确认 `cmp` 对相同 daemon 返回 0，对单字节差异返回 1。
- [x] 所有 Shell 文件通过 Git Bash `bash -n` 语法检查。
- [x] Lua PGM 渲染器已实际生成并由 Pillow 解码 640×260 灰度测试图。
- [x] 测试覆盖矩形、圆角选择块、黑白文字、中文、数字、百分比和 PGM 头。
- [x] 字形素材可由随包 Noto CJK 字体通过 `tools/generate_dynamic_glyphs.py` 重建。
- [x] 3 张全屏背景为 1272×1696，31 张日期素材为 105×78，字形 PGM 可正常解码。
- [x] `RUNME.sh` 仍使用 `;log runme`，并指向事务式优化版安装器。
- [x] 安装器只在完整暂存、载荷校验和 TSV 备份后停止服务。
- [x] `reading-time.tsv` 从未作为删除、移动或替换目标。
- [x] `reading-time.tsv.bak` 已存在时会原样保留。
- [x] `cc.db` 访问只存在于首次进入书籍页调用的 `ensure_progress` 中。
- [x] 重复 Tab、重复日期和边界翻页走零重绘分支。

## FBInk 调用数设计对比

下表包含最终屏幕刷新调用。原版数量随当月阅读天数、书籍进度和有数据的
柱状图数量变化；优化版上限按 3 条每日书名或 5 条书籍标题计算。

| 操作 | 原始 9.6.3 调用数 | 优化版目标上限 | 预计下降 |
|---|---:|---:|---:|
| 首次打开累计页 | 22～46 | 5 | 77%～89% |
| 累计时长 → 每日时长 | 34～74 | 10 | 71%～86% |
| 每日时长 → 阅读书籍 | 18～28 | 9 | 50%～68% |
| 点击不同日期 | 34～74，整页重画 | 7，局部 | 79%～91% |
| 月份切换 | 34～74，整页重画 | 9，局部 | 74%～88% |
| 书籍翻页 | 18～28，整页重画 | 8，局部 | 56%～71% |
| 重复 Tab／边界翻页 | 与对应页面相同 | 0 | 100% |

这是代码路径计数，不冒充 KPW6 实际耗时。优化版会在
`dashboard-launch.log` 中为每次操作写入：

```text
METRIC operation=... elapsed_ms=... fbink_calls=... renderer=... fallback=... redraw=...
```

## KPW6 真机量化验收（待实机）

必须使用同一台 1272×1696 KPW6 和同一份 `reading-time.tsv` 分别记录原版与
优化版。当前工作区不能模拟 Kindle framebuffer、LIPC、Upstart 或墨水屏波形，
因此下面的实际耗时不得在电脑端伪造。

| 操作 | 原版实际耗时 | 优化版实际耗时 | 原版调用数 | 优化版调用数 | 结论 |
|---|---:|---:|---:|---:|---|
| 首次打开 Dashboard | 待测 | 待测 | 待测 | 待测 | 待真机 |
| 累计时长 → 每日时长 | 待测 | 待测 | 待测 | 待测 | 待真机 |
| 每日时长 → 阅读书籍 | 待测 | 待测 | 待测 | 待测 | 待真机 |
| 点击日期 | 待测 | 待测 | 待测 | 待测 | 待真机 |
| 月份切换 | 待测 | 待测 | 待测 | 待测 | 待真机 |
| 书籍翻页 | 待测 | 待测 | 待测 | 待测 | 待真机 |

真机还需确认：

- 触摸区域与缩放后画面一致，所有中文书名正常。
- 普通交互使用局部 GC16_FAST，第 6 次有效绘制执行 GC16 清残影。
- `DASHBOARD_RENDERER_FORCE_FAIL=1` 前台启动时切换到原始 9.6.3 页面，仍可退出。
- 退出后不存在新增 Lua、Shell 或渲染器进程。
- `/tmp/native-reading-dashboard.*` 无本次会话残留。
- `native-reading-time` 仍只有原实例，轮询和数据写入行为不变。
- 退出正常返回 Kindle 原生书库，历史 TSV 及首次备份完整。
